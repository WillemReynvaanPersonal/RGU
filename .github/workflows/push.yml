on: push

jobs:
  install-poetry:
    uses: ./.github/workflows/install_poetry.yml

  flake8:
    needs: install-poetry
    name: Check flake8
    runs-on: ubuntu-latest
    steps:
      - uses: install-poetry
      - run: poetry run flake8
